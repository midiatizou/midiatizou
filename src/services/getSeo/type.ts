export type dataTypes = {
  title: [
    {
      type: string;
      text: string;
      spans: [];
    }
  ];
  meta_description: [
    {
      type: string;
      text: string;
      spans: [];
    }
  ];
};
